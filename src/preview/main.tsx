import { createRoot } from 'react-dom/client';
import DeepDivePreview from './DeepDivePreview.tsx';
import '../styles/index.css';

createRoot(document.getElementById('root')!).render(<DeepDivePreview />);
